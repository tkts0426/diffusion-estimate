#ifndef calculateFZ
#define calculateFZ

#include <random>
#include <vector>

class CalculateFZ{
    std::vector<double> estimateSigma(int n, std::vector<double> process, std::vector<double> partition);

};

#endif